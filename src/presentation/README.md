# presentation 層

ユーザーに見せ、ユーザーの操作を受け取る層。React コンポーネントを置く。

## 置くもの

- React コンポーネント
- カスタムフック
- DI のための Context

## 依存してよいもの

application 層（ユースケース）と、自分自身。

## 判断の目安

この層は**薄い**ほどよい。判断やルールを持たず、
「ユーザーの操作をユースケースの呼び出しに変換し、返ってきた DTO を描画する」だけにする。
コンポーネントの中に `if` が増えてきたら、その判断は domain 層に属していないかを疑う。
